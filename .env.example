# =============================================================================
# SISTEMA DE AGENDAMENTOS CRAS - Frontend Environment Variables
# =============================================================================
# ⚠️ IMPORTANTE: Este é um arquivo de EXEMPLO. NÃO coloque valores reais aqui!
# 
# 1. Copie este arquivo: cp .env.example .env.local (para Vite)
# 2. Edite o arquivo .env.local com seus valores reais
# 3. NUNCA commite o arquivo .env.local no Git
# =============================================================================

# -----------------------------------------------------------------------------
# 🌐 FRONTEND CONFIGURATION
# -----------------------------------------------------------------------------
# URL da API backend (usado pelo Vite para proxying)
# ⚠️ Variáveis do Vite DEVEM começar com VITE_ para serem expostas ao cliente
VITE_API_URL=http://localhost:5000

# Em produção:
# VITE_API_URL=https://api.seudominio.com.br

# ⚠️ IMPORTANTE:
# - Em produção, SEMPRE use HTTPS
# - Nunca exponha secrets/passwords em variáveis VITE_* (elas são públicas no bundle!)
# - Apenas URLs públicas devem estar aqui

# -----------------------------------------------------------------------------
# 🔧 DEVELOPMENT CONFIGURATION (opcional)
# -----------------------------------------------------------------------------
# Habilitar debug mode (valores: true, false)
# VITE_DEBUG=false

# Timeout para requisições HTTP (em ms)
# VITE_API_TIMEOUT=30000

# -----------------------------------------------------------------------------
# OBSERVAÇÕES IMPORTANTES
# -----------------------------------------------------------------------------
# 
# ⚠️ Variáveis VITE_* são PÚBLICAS e expostas no bundle JavaScript!
# ⚠️ NUNCA coloque secrets, senhas ou tokens aqui!
# ⚠️ Apenas URLs e configurações públicas devem estar neste arquivo!
# 
# Para desenvolvimento local:
# - Use .env.local (não commitado)
# 
# Para produção:
# - Configure as variáveis no seu serviço de hosting (Vercel, Netlify, etc.)
# 
# =============================================================================

